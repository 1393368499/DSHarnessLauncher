{
  "name": "dsharness",
  "version": "4.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "tauri": "tauri",
    "bundle": "npm run build && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.9.1"
  }
}
